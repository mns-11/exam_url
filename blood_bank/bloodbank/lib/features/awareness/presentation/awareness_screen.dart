import 'package:flutter/material.dart';

class AwarenessCenterScreen extends StatelessWidget {
  const AwarenessCenterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('مركز التوعية')),
      body: const Center(child: Text('Awareness Placeholder')),
    );
  }
}
